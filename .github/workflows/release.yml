name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci
        
      - name: Build app
        run: npm run build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Chrome Extension ZIP
        run: |
          cd extensions/chrome
          zip -r ../../dist/siteseeker-chrome-extension.zip ./*

      - name: Create Firefox Extension ZIP
        run: |
          cd extensions/firefox
          zip -r ../../dist/siteseeker-firefox-extension.zip ./*

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/SiteSeeker-*.dmg
            dist/SiteSeeker-*.zip
            dist/*.blockmap
            dist/latest-mac.yml
            dist/siteseeker-chrome-extension.zip
            dist/siteseeker-firefox-extension.zip
          draft: false
          prerelease: false
          generate_release_notes: true 